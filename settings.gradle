/*
Enable downloading the JDK version specified in the toolchain if it's
not available in the environment.
 */
plugins {
  id("org.gradle.toolchains.foojay-resolver-convention") version "0.7.0"
}

rootProject.name = "fruitfly"

